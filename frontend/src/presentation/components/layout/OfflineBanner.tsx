'use client'

/**
 * OfflineBanner — substituído pela lógica do SyncWidget no AppShell.
 * Mantido como stub para compatibilidade com imports existentes.
 */
export function OfflineBanner() {
  return null
}
